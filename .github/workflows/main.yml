name: Schedule Worker
on:
  workflow_dispatch:
  push:
    branches:
      - master #2020年10月后github新建仓库默认分支改为main，注意更改
jobs:
  run-python-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.11.4'
      - run: python main.py
      - run: wget https://github.com/studyzy/imewlconverter/releases/download/v3.1.0/imewlconverter_Linux.tar.gz
      - run: tar -zxvf imewlconverter_Linux.tar.gz
      - run: dotnet publish/ImeWlConverterCmd.dll -i:word word/1234.txt -o:gboard cs/gg.txt

      - name: Archive files
        uses: actions/upload-artifact@v2
        with:
          name: compressed-files
          path: cs
